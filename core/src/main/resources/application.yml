server:
  port: 8081

spring:
  application:
    name: spot-core

  datasource:
    url: jdbc:mysql://localhost:3306/spot_core
    username: root
    password: 5167
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
